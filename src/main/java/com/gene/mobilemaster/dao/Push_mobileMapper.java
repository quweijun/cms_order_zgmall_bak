package com.gene.mobilemaster.dao;

import com.gene.mobilemaster.model.Push_mobile;

public interface Push_mobileMapper {
    int insert(Push_mobile record);

    int insertSelective(Push_mobile record);
}